# =============================================================================
# CI Pipeline - Restaurant Service
# =============================================================================
# Builds and tests the Restaurant bounded context (service + API module).
# Path-based filtering ensures this workflow only runs when relevant files change.
# =============================================================================

name: "CI: Restaurant Service"

on:
  push:
    branches:
      - main
      - master
      - 'feat/microservices-migration-*'
    paths:
      - 'services/restaurant-service/**'
      - 'services/restaurant-service-api/**'
      - 'ftgo-restaurant-service/**'
      - 'ftgo-restaurant-service-api/**'
      - 'shared-libraries/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'
      - '.github/workflows/ci-restaurant-service.yml'
  pull_request:
    branches:
      - main
      - master
      - 'feat/microservices-migration-*'
    paths:
      - 'services/restaurant-service/**'
      - 'services/restaurant-service-api/**'
      - 'ftgo-restaurant-service/**'
      - 'ftgo-restaurant-service-api/**'
      - 'shared-libraries/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'
      - '.github/workflows/ci-restaurant-service.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Restaurant Service API
        run: ./gradlew :services:restaurant-service-api:build

      - name: Build & Test Restaurant Service
        run: ./gradlew :services:restaurant-service:build

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: restaurant-service-test-reports
          path: |
            services/restaurant-service/build/reports/tests/
            services/restaurant-service/build/test-results/
            services/restaurant-service-api/build/reports/tests/
            services/restaurant-service-api/build/test-results/
          retention-days: 14

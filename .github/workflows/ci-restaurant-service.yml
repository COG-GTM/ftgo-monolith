# =============================================================================
# FTGO Restaurant Service - CI Build and Test
# =============================================================================
# Builds and tests the Restaurant Service on PRs and pushes to main branches.
# Triggers only when restaurant-service or shared library code changes.
# =============================================================================

name: "CI: Restaurant Service"

on:
  push:
    branches:
      - main
      - master
      - 'feat/microservices-migration'
    paths:
      - 'ftgo-restaurant-service/**'
      - 'ftgo-restaurant-service-api/**'
      - 'services/ftgo-restaurant-service/**'
      - 'ftgo-common/**'
      - 'ftgo-common-jpa/**'
      - 'ftgo-domain/**'
      - 'shared/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'
      - 'gradle/libs.versions.toml'
      - 'buildSrc/**'
      - '.github/workflows/ci-restaurant-service.yml'
  pull_request:
    branches:
      - main
      - master
      - 'feat/microservices-migration'
    paths:
      - 'ftgo-restaurant-service/**'
      - 'ftgo-restaurant-service-api/**'
      - 'services/ftgo-restaurant-service/**'
      - 'ftgo-common/**'
      - 'ftgo-common-jpa/**'
      - 'ftgo-domain/**'
      - 'shared/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'
      - 'gradle/libs.versions.toml'
      - 'buildSrc/**'
      - '.github/workflows/ci-restaurant-service.yml'

permissions:
  contents: read

jobs:
  build:
    name: Build & Test Restaurant Service
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Compile Restaurant Service
        run: |
          ./gradlew :ftgo-restaurant-service:compileJava \
            :ftgo-restaurant-service-api:compileJava \
            -x :ftgo-end-to-end-tests-common:compileJava \
            -x :ftgo-end-to-end-tests:compileJava

      - name: Run Restaurant Service Tests
        env:
          # JDK 17+ module access flags required by legacy Mockito/Spring test libraries
          _JAVA_OPTIONS: >-
            --add-opens java.base/java.lang=ALL-UNNAMED
            --add-opens java.base/java.lang.reflect=ALL-UNNAMED
            --add-opens java.base/java.util=ALL-UNNAMED
        run: |
          ./gradlew :ftgo-restaurant-service:test \
            -x :ftgo-end-to-end-tests-common:test \
            -x :ftgo-end-to-end-tests:test \
            -x :ftgo-application:test

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: restaurant-service-test-reports
          path: |
            ftgo-restaurant-service/build/reports/tests/
            ftgo-restaurant-service/build/test-results/
          retention-days: 14

      - name: Generate build summary
        if: always()
        run: |
          echo "## CI: Restaurant Service" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -d "ftgo-restaurant-service/build/reports/tests/test" ]; then
            echo "Test reports uploaded as artifacts." >> $GITHUB_STEP_SUMMARY
          else
            echo "No test reports generated." >> $GITHUB_STEP_SUMMARY
          fi

# =============================================================================
# CI Pipeline - Consumer Service
# =============================================================================
# Builds and tests the Consumer bounded context (service + API module).
# Triggers on changes to the service code, shared libraries, or build config.
# =============================================================================

name: "CI: Consumer Service"

on:
  push:
    branches:
      - main
      - master
      - 'feat/microservices-migration-*'
    paths:
      - 'services/consumer-service/**'
      - 'services/consumer-service-api/**'
      - 'ftgo-consumer-service/**'
      - 'ftgo-consumer-service-api/**'
      - 'shared-libraries/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'
  pull_request:
    branches:
      - main
      - master
      - 'feat/microservices-migration-*'
    paths:
      - 'services/consumer-service/**'
      - 'services/consumer-service-api/**'
      - 'ftgo-consumer-service/**'
      - 'ftgo-consumer-service-api/**'
      - 'shared-libraries/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle.properties'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-consumer-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-consumer-
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Consumer Service API
        run: ./gradlew :services:consumer-service-api:build

      - name: Build Consumer Service
        run: ./gradlew :services:consumer-service:build

      - name: Run Consumer Service unit tests
        run: ./gradlew :services:consumer-service:test :services:consumer-service-api:test

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: consumer-service-test-reports
          path: |
            services/consumer-service/build/reports/tests/
            services/consumer-service-api/build/reports/tests/
          retention-days: 14

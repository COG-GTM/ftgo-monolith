# =============================================================================
# CI Pipeline - Consumer Service
# =============================================================================
# Builds and tests the Consumer bounded context on PRs and pushes to main.
# Path-based filtering ensures this workflow only runs when relevant files change.
# =============================================================================

name: "CI: Consumer Service"

on:
  push:
    branches: [main]
    paths:
      - 'services/consumer-service/**'
      - 'services/consumer-service-api/**'
      - 'shared-libraries/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
      - '.github/workflows/ci-consumer-service.yml'
  pull_request:
    branches: [main]
    paths:
      - 'services/consumer-service/**'
      - 'services/consumer-service-api/**'
      - 'shared-libraries/**'
      - 'buildSrc/**'
      - 'gradle/**'
      - 'build.gradle'
      - 'settings.gradle'
      - '.github/workflows/ci-consumer-service.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Consumer Service API
        run: ./gradlew :services:consumer-service-api:build

      - name: Build & Test Consumer Service
        run: ./gradlew :services:consumer-service:build

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: consumer-service-test-reports
          path: |
            services/consumer-service/build/reports/tests/
            services/consumer-service/build/test-results/
          retention-days: 14

plugins {
    id "org.flywaydb.flyway" version "6.0.0"
}

flyway {
    url = "jdbc:mysql://${System.getenv('MYSQL_HOST') ?: '127.0.0.1'}/ftgo?useSSL=false"
    user = 'root'
    password = System.getenv('MYSQL_ROOT_PASSWORD') ?: 'rootpassword'
    schemas = ['ftgo']
}

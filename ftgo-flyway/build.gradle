plugins {
    id "org.flywaydb.flyway" version "6.0.0"
}

flyway {
    url = "jdbc:mysql://${System.getenv('DOCKER_HOST_IP') ?: 'localhost'}/ftgo?sslMode=REQUIRED"
    user = 'root'
    password = 'rootpassword'
    schemas = ['ftgo']
}

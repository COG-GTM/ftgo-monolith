plugins {
    id "org.flywaydb.flyway" version "6.0.0"
}

flyway {
    url = "jdbc:postgresql://${System.getenv('DOCKER_HOST_IP') ?: 'localhost'}:5432/ftgo"
    user = 'ftgouser'
    password = 'ftgopw'
    schemas = ['public']
}

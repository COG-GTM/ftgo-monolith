apiVersion: v1
kind: Service
metadata:
  name: zipkin
  namespace: ftgo-tracing
  labels:
    app: zipkin
    app.kubernetes.io/name: zipkin
    app.kubernetes.io/part-of: ftgo
    app.kubernetes.io/component: tracing
spec:
  type: ClusterIP
  selector:
    app: zipkin
  ports:
    - name: http
      port: 9411
      targetPort: 9411
      protocol: TCP

apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: ftgo-monitoring
  labels:
    app: grafana
    app.kubernetes.io/part-of: ftgo
type: Opaque
stringData:
  # Override these values before deploying to production.
  # Use: kubectl create secret generic grafana-credentials \
  #   --from-literal=admin-user=<USER> --from-literal=admin-password=<PASSWORD>
  admin-user: CHANGE_ME
  admin-password: CHANGE_ME

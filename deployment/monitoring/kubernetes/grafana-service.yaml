apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: ftgo-monitoring
  labels:
    app: grafana
    app.kubernetes.io/part-of: ftgo
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: grafana

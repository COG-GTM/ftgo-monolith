---
# NOTE: This file contains placeholder base64-encoded values for development only.
# In production, use SealedSecrets or an external secrets operator to manage credentials.
# See infrastructure/kubernetes/base/sealed-secrets/ for production secret management.
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-db-secret
  labels:
    app.kubernetes.io/name: ftgo-mysql
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: ftgo
type: Opaque
data:
  # These are placeholder values - override via Kustomize overlays or SealedSecrets
  root-password: REPLACE_VIA_SEALED_SECRET
  username: REPLACE_VIA_SEALED_SECRET
  password: REPLACE_VIA_SEALED_SECRET
---

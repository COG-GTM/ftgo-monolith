---
# =============================================================================
# MySQL Database Secret
# =============================================================================
# WARNING: This file is a TEMPLATE. Do NOT commit actual secret values.
# For production, use external-secrets or sealed-secrets (see deploy/helm/).
#
# To create this secret manually for local/dev use:
#   kubectl create secret generic ftgo-db-secret \
#     --from-literal=root-password=<ROOT_PASSWORD> \
#     --from-literal=username=<DB_USER> \
#     --from-literal=password=<DB_PASSWORD> \
#     --from-literal=database=ftgo
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-db-secret
  labels:
    app.kubernetes.io/name: ftgo-mysql
    app.kubernetes.io/part-of: ftgo-platform
type: Opaque
# Secret data must be provided at deploy time via:
#   - external-secrets operator (recommended)
#   - sealed-secrets
#   - kubectl create secret (dev/local only)
# See deploy/helm/ftgo/templates/external-secrets.yaml for automated secret management.
---

apiVersion: v1
kind: Service
metadata:
  name: order-mysql
  labels:
    name: order-mysql
spec:
  ports:
  - port: 3306
    targetPort: 3306
  clusterIP: None
  selector:
    role: order-mysql
---
apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: order-mysql
spec:
  serviceName: "order-mysql"
  replicas: 1
  template:
    metadata:
      labels:
        role: order-mysql
    spec:
      terminationGracePeriodSeconds: 10
      containers:
        - name: order-mysql
          image: mysql:5.7
          ports:
              - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-mysql-secret
                  key: root-password
            - name: MYSQL_DATABASE
              value: ftgo_order
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: order-mysql-secret
                  key: username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-mysql-secret
                  key: password
          volumeMounts:
            - name: order-mysql-storage
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: order-mysql-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---

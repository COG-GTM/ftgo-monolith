# TEMPLATE ONLY — do NOT commit real credentials and do NOT `kubectl apply` this file directly.
# Create the real Secret out-of-band with create-db-secret.sh, which reads the
# credentials from environment variables so they are never stored in git.
---
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-db-secret
type: Opaque
stringData:
  root-password: CHANGE_ME
  username: mysqluser
  password: CHANGE_ME
---

# =============================================================================
# FTGO Platform - Gradle Properties
# =============================================================================
# Legacy version properties are maintained for backward compatibility with
# monolith modules. New microservices should use the Gradle version catalog
# (gradle/libs.versions.toml) instead of these properties.
# =============================================================================

deployUrl=file:///Users/cer/.m2/testdeploy

eventuateMavenRepoUrl=https://dl.bintray.com/eventuateio-oss/eventuate-maven-release,file:///Users/cer/.m2/testdeploy

# --- Legacy Module Versions (backward compatibility) ---
# New microservices should use libs.versions.toml instead
springBootVersion=2.7.18
restAssuredVersion=2.9.0
springDependencyManagementPluginVersion=1.1.4
eventuateUtilVersion=0.1.0.RELEASE

dockerComposePluginVersion=0.14.13
micrometerVersion=1.0.4

# --- Gradle Build Settings ---
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

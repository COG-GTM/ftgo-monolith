# ============================================================================
# FTGO Microservices Platform - Gradle Properties
# ============================================================================
# NOTE: Dependency versions are now centralized in gradle/libs.versions.toml
# (Gradle Version Catalog). Only build-level properties remain here.
# ============================================================================

# Legacy properties preserved for backward compatibility with e2e test modules.
# These will be removed once all legacy modules are fully migrated.
eventuateUtilVersion=0.1.0.RELEASE
restAssuredVersion=5.4.0

dependencies {
    api project(":ftgo-common")
    api project(":ftgo-common-jpa")

    // Version managed by the Boot BOM (JUnit 4 until the tests are migrated to Jupiter).
    testImplementation "junit:junit"
}
// =============================================================================
// ftgo-consumer-service-api — Consumer Service API DTOs for cross-service communication
// Contains request/response DTOs for the Consumer Service
// Migrated from monolith module: ftgo-consumer-service-api
// =============================================================================

version = '1.0.0'

apply plugin: 'maven-publish'

dependencies {
    compile project(':shared-ftgo-common')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java

            groupId = 'net.chrisrichardson.ftgo'
            artifactId = 'ftgo-consumer-service-api'
            version = project.version

            pom {
                name = 'FTGO Consumer Service API'
                description = 'API DTOs for cross-service communication with the FTGO Consumer Service.'
            }
        }
    }

    repositories {
        maven {
            name = 'local'
            url = "${rootProject.buildDir}/repo"
        }
    }
}

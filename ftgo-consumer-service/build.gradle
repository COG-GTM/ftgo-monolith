dependencies {
    api project(":common-swagger")
    api project(":ftgo-consumer-service-api")
    api project(":ftgo-domain")

    api "org.springframework.boot:spring-boot-starter-data-jpa"
    api "org.springframework.boot:spring-boot-starter-web"
    api "org.springframework.boot:spring-boot-starter-actuator"

    // Bean Validation (Hibernate Validator + Jakarta EL) replaces the old javax.el-api dependency.
    api "org.springframework.boot:spring-boot-starter-validation"

    // JUnit 5 (Jupiter), Mockito, AssertJ and Spring MockMvc for the unit tests.
    testImplementation "org.springframework.boot:spring-boot-starter-test"
    // io.rest-assured 5.x replaces com.jayway.restassured; spring-mock-mvc drives the
    // controller tests through MockMvc without starting a servlet container.
    testImplementation "io.rest-assured:rest-assured:$restAssuredVersion"
    testImplementation "io.rest-assured:spring-mock-mvc:$restAssuredVersion"
    testImplementation "io.rest-assured:json-path:$restAssuredVersion"
    testImplementation "com.jayway.jsonpath:json-path"
    // Gradle 8 no longer ships the JUnit Platform launcher on the test runtime classpath;
    // the version is managed by the Boot BOM so it stays aligned with junit-platform-engine.
    testRuntimeOnly "org.junit.platform:junit-platform-launcher"
}

# =============================================================================
# FTGO Docker Compose Environment Variables
# =============================================================================
# Copy this file to .env and update the values:
#   cp docker/.env.example docker/.env
# =============================================================================

# MySQL Configuration
MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_USER=ftgouser
MYSQL_PASSWORD=changeme_password

# Image tag override (for docker-compose.registry.yml)
# IMAGE_TAG=latest

apply plugin: FtgoServicePlugin

dependencies {
    // Self-contained extracted service: it owns decoupled copies of the Courier/Action/Plan
    // entities (see this module's domain package) so it does NOT depend on ftgo-courier-service
    // or ftgo-domain, keeping the entangled Order/Action coupling out of the standalone service.
    compile project(":ftgo-courier-service-api")
    compile project(":ftgo-common")

    compile "org.springframework.boot:spring-boot-starter-web:$springBootVersion"
    compile "org.springframework.boot:spring-boot-starter-data-jpa:$springBootVersion"
    compile "org.springframework.boot:spring-boot-starter-actuator:$springBootVersion"

    compile "com.h2database:h2:1.4.197"
    compile "org.flywaydb:flyway-core:5.0.7"

    testCompile "org.springframework.boot:spring-boot-starter-test:$springBootVersion"
}

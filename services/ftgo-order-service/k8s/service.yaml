apiVersion: v1
kind: Service
metadata:
  name: ftgo-order-service
  labels:
    app: ftgo-order-service
    part-of: ftgo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: ftgo-order-service

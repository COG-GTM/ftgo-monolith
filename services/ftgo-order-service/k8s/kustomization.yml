apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Per-service Kustomization
# This enables service-level customization while referencing the shared base.
# Usage: kubectl apply -k services/ftgo-order-service/k8s/

resources:
  - ../../../infrastructure/kubernetes/base/order-service

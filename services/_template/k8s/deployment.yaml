apiVersion: apps/v1
kind: Deployment
metadata:
  name: ftgo-template-service
  labels:
    app: ftgo-template-service
    part-of: ftgo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ftgo-template-service
  template:
    metadata:
      labels:
        app: ftgo-template-service
    spec:
      containers:
        - name: ftgo-template-service
          image: ftgo/ftgo-template-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ftgo-template-db-secret
                  key: password
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi

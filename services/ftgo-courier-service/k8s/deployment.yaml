apiVersion: apps/v1
kind: Deployment
metadata:
  name: ftgo-courier-service
  labels:
    app: ftgo-courier-service
    part-of: ftgo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ftgo-courier-service
  template:
    metadata:
      labels:
        app: ftgo-courier-service
    spec:
      containers:
        - name: ftgo-courier-service
          image: ftgo/ftgo-courier-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ftgo-courier-db-secret
                  key: password
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi

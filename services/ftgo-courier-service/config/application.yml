# FTGO Courier Service Configuration
# This file contains default configuration for the Courier microservice.
# Environment-specific overrides should be placed in application-{profile}.yml

server:
  port: 8084

spring:
  application:
    name: ftgo-courier-service
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_courier_service
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

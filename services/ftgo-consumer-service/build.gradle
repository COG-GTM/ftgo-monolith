// Microservices version of ftgo-consumer-service
// This module will eventually replace the monolith's ftgo-consumer-service
// During migration, both coexist side by side.

apply plugin: 'io.spring.dependency-management'
apply plugin: 'ftgo.quality-gate-conventions'

// Configure Java 17 for test compilation and execution (required by ftgo-test-lib / Spring Boot 3.x)
def java17Home = ['/usr/lib/jvm/java-17-openjdk-amd64', '/usr/lib/jvm/java-1.17.0-openjdk-amd64'].find { new File(it).exists() }
if (java17Home) {
    compileTestJava {
        sourceCompatibility = '17'
        targetCompatibility = '17'
        options.fork = true
        options.forkOptions.javaHome = file(java17Home)
    }
    test {
        executable = "${java17Home}/bin/java"
        jvmArgs = ['--add-opens', 'java.base/java.lang=ALL-UNNAMED']
        useJUnitPlatform()
    }
}

dependencies {
    // Shared libraries (uncomment as migration progresses)
    // compile project(':shared-ftgo-common')
    // compile project(':shared-ftgo-domain')

    // Spring Boot
    compile "org.springframework.boot:spring-boot-starter-web:$springBootVersion"
    compile "org.springframework.boot:spring-boot-starter-data-jpa:$springBootVersion"
    compile "org.springframework.boot:spring-boot-starter-actuator:$springBootVersion"

    // Security
    compile project(':shared-ftgo-security-lib')

    // Distributed Tracing (Micrometer Tracing + Brave + Zipkin)
    compile project(':shared-ftgo-tracing-lib')

    // Testing
    testCompile "org.springframework.boot:spring-boot-starter-test:$springBootVersion"
    testCompile "org.springframework.security:spring-security-test:6.2.4"
    testCompile project(':shared-ftgo-test-lib')
}

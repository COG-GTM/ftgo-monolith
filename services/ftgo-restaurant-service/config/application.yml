# FTGO Restaurant Service Configuration
# This file contains default configuration for the Restaurant microservice.
# Environment-specific overrides should be placed in application-{profile}.yml

server:
  port: 8083

spring:
  application:
    name: ftgo-restaurant-service
  datasource:
    url: jdbc:mysql://localhost:3306/ftgo_restaurant_service
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

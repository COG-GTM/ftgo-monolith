apiVersion: v1
kind: Service
metadata:
  name: ftgo-template-service
  labels:
    app: ftgo-template-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ftgo-template-service

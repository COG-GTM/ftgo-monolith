apiVersion: v1
kind: Service
metadata:
  name: consumer-service
  labels:
    app: consumer-service
    part-of: ftgo
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: http
      protocol: TCP
  selector:
    app: consumer-service

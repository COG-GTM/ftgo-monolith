# Copy to .env and set strong values before running docker-compose.
# .env is gitignored so real credentials never get committed.
# The DB username is fixed to 'mysqluser' (see mysql/schema.sql); only passwords are secrets.
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_PASSWORD=change-me-app

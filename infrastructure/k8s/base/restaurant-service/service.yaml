apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  labels:
    app.kubernetes.io/name: restaurant-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: restaurant-service

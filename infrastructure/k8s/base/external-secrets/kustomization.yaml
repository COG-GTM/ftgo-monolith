apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# External Secrets resources - only include when external-secrets operator is installed
# To enable: add this directory to the base kustomization.yaml resources
resources:
  - cluster-secret-store.yaml
  - external-secret-mysql.yaml
  - external-secret-services.yaml

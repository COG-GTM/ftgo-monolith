# ============================================================
# FTGO MySQL Secret - Placeholder for sealed-secrets/external-secrets
# ============================================================
# WARNING: This file contains placeholder values only.
# In production, replace with SealedSecret or ExternalSecret resources.
# See infrastructure/k8s/base/external-secrets/ for ExternalSecret templates.
#
# To create real secrets:
#   kubectl create secret generic ftgo-mysql-secret \
#     --from-literal=root-password=<ROOT_PASSWORD> \
#     --from-literal=username=<USERNAME> \
#     --from-literal=password=<PASSWORD> \
#     -n <NAMESPACE>
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-mysql-secret
  labels:
    app.kubernetes.io/name: ftgo-mysql
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: ftgo-platform
  annotations:
    kubernetes.io/description: "MySQL credentials - replace with SealedSecret or ExternalSecret in production"
type: Opaque
# Placeholder values (base64-encoded) - MUST be replaced per environment
# These are overridden by environment overlays or external secret management
stringData:
  root-password: "REPLACE_ME"
  username: "REPLACE_ME"
  password: "REPLACE_ME"

# ============================================================
# FTGO API Gateway - Secret
# ============================================================
# WARNING: Placeholder values only. Replace with SealedSecret
# or ExternalSecret in production environments.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-gateway-secret
  labels:
    app.kubernetes.io/name: ftgo-api-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: ftgo-platform
  annotations:
    kubernetes.io/description: "API Gateway secrets - replace with SealedSecret or ExternalSecret"
type: Opaque
stringData:
  jwt-secret: "REPLACE_ME_WITH_REAL_SECRET_AT_LEAST_256_BITS"

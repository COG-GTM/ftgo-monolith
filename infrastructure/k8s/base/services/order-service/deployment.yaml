# ============================================================
# FTGO Order Service - Deployment + Service
# ============================================================
# Mirror of services/ftgo-order-service/k8s/deployment.yaml
# for Kustomize compatibility (must be within base directory).
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ftgo-order-service
  labels:
    app: ftgo-order-service
    app.kubernetes.io/name: ftgo-order-service
    app.kubernetes.io/component: service
    version: "1.0.0"
    team: ftgo-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ftgo-order-service
  template:
    metadata:
      labels:
        app: ftgo-order-service
        app.kubernetes.io/name: ftgo-order-service
        version: "1.0.0"
    spec:
      containers:
        - name: ftgo-order-service
          image: ftgo/ftgo-order-service:latest
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: ftgo-order-db-secret
                  key: url
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ftgo-order-db-secret
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ftgo-order-db-secret
                  key: password
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: ftgo-order-service
  labels:
    app: ftgo-order-service
    app.kubernetes.io/name: ftgo-order-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: ftgo-order-service

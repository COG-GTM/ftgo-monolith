# ============================================================
# FTGO Courier Service - Database Secret
# ============================================================
# WARNING: Placeholder values only. Replace with SealedSecret
# or ExternalSecret in production environments.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-courier-db-secret
  labels:
    app.kubernetes.io/name: ftgo-courier-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: ftgo-platform
  annotations:
    kubernetes.io/description: "Courier service DB credentials - replace with SealedSecret or ExternalSecret"
type: Opaque
stringData:
  url: "jdbc:mysql://ftgo-mysql:3306/ftgo?useSSL=false&allowPublicKeyRetrieval=true"
  username: "REPLACE_ME"
  password: "REPLACE_ME"

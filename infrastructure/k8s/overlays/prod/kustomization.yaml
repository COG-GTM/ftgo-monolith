apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ftgo-prod

resources:
  - ../../base

namePrefix: prod-

commonLabels:
  environment: production

patchesStrategicMerge: []
# Add environment-specific patches here (replica counts, resource limits, etc.)

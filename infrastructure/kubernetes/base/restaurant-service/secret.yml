---
# NOTE: Placeholder secret - replace with SealedSecrets in production.
# Never commit real credentials to version control.
apiVersion: v1
kind: Secret
metadata:
  name: ftgo-restaurant-service-secret
  labels:
    app.kubernetes.io/name: ftgo-restaurant-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: ftgo
type: Opaque
data:
  # Placeholder: override via Kustomize overlays or SealedSecrets per environment
  db-username: REPLACE_VIA_SEALED_SECRET
  db-password: REPLACE_VIA_SEALED_SECRET
